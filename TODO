Oct 17, 2015
    Make grid draw, selector draw, stones draw, state record
    Check for state validity, add pass to move options
    add a scoreboard
    add a logger
    refactor much of Board functionality to utils.py
    add validation functions to classes
    add __repr__
    overwride __eq__
    fix bug where it thinks a move is invalid if a stone is surrounded by its
        own allies
    fix non-capture bug
